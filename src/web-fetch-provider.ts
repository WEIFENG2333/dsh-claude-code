/** Bundle-resolvable wrapper for DSH's maintained HTTP fetch provider. */

export {
  apply,
  Config,
  inject,
  name,
} from '@deepseek-ai/dsh-web-fetch-http'
export type { Config as WebFetchProviderConfig } from '@deepseek-ai/dsh-web-fetch-http'
